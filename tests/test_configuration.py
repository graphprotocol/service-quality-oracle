"""
Unit tests for the configuration loader and validator.
"""

# TODO: Test successful config loading using `config.toml.example`.
# TODO: Test `ConfigurationError` is raised when `config.toml` is missing.
# TODO: Test `ConfigurationError` is raised for a malformed TOML file.
# TODO: Test successful substitution of an environment variable.
# TODO: Test `ConfigurationError` is raised when a required environment variable is not set.
# TODO: Test `_validate_config` raises an error for missing required fields.
# TODO: Test `_validate_config` raises an error for an invalid `SCHEDULED_RUN_TIME` format.
# TODO: Test `get_missing_env_vars` correctly identifies all unset variables.
# TODO: Test `CredentialManager` with valid inline service account JSON.
# TODO: Test `CredentialManager` with valid inline authorized user JSON.
# TODO: Test `CredentialManager` with a valid file path.
# TODO: Test `CredentialManager` raises `ValueError` for invalid or incomplete JSON.
# TODO: Test `validate_all_required_env_vars` passes when all variables are set.
# TODO: Test `validate_all_required_env_vars` fails when variables are missing.
