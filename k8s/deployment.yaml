apiVersion: apps/v1
kind: Deployment
metadata:
  name: rewards-eligibility-oracle
  labels:
    app: rewards-eligibility-oracle
spec:
  replicas: 1  # Single instance due to state management
  selector:
    matchLabels:
      app: rewards-eligibility-oracle
  template:
    metadata:
      labels:
        app: rewards-eligibility-oracle
    spec:
      # The oracle never calls the Kubernetes API, so it does not need a service account token
      automountServiceAccountToken: false
      containers:
      - name: rewards-eligibility-oracle
        image: ghcr.io/graphprotocol/rewards-eligibility-oracle:v0.4.11 # x-release-please-version
        env:
        # Runtime-only configuration (not in config.toml)
        - name: RUN_ON_STARTUP
          value: "true"
        # Secrets from Kubernetes Secret
        - name: GOOGLE_APPLICATION_CREDENTIALS
          valueFrom:
            secretKeyRef:
              name: rewards-eligibility-oracle-secrets
              key: google-credentials
        - name: BLOCKCHAIN_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: rewards-eligibility-oracle-secrets
              key: blockchain-private-key
        - name: ETHERSCAN_API_KEY
          valueFrom:
            secretKeyRef:
              name: rewards-eligibility-oracle-secrets
              key: etherscan-api-key
        - name: ARBITRUM_API_KEY
          valueFrom:
            secretKeyRef:
              name: rewards-eligibility-oracle-secrets
              key: arbitrum-api-key
        - name: SLACK_WEBHOOK_URL
          valueFrom:
            secretKeyRef:
              name: rewards-eligibility-oracle-secrets
              key: slack-webhook-url
        - name: OPSGENIE_API_KEY
          valueFrom:
            secretKeyRef:
              name: rewards-eligibility-oracle-secrets
              key: opsgenie-api-key
        volumeMounts:
        - name: config-volume
          mountPath: /app/config.toml
          subPath: config.toml
          readOnly: true
        - name: data-volume
          mountPath: /app/data
        - name: logs-volume
          mountPath: /app/logs
        resources:
          requests:
            memory: "512M"  # Match docker-compose reservations
            cpu: "250m"
            ephemeral-storage: "256Mi"
          limits:
            memory: "1G"    # Match docker-compose limits
            cpu: "1000m"    # Match docker-compose '1.0' cpus
            ephemeral-storage: "1Gi"  # Data and logs live on persistent volumes, so scratch use stays small
        # Use file-based healthcheck like docker-compose (not HTTP)
        livenessProbe:
          exec:
            command:
            - python
            - -c
            - "import os, time; assert os.path.exists('/app/healthcheck') and time.time() - os.path.getmtime('/app/healthcheck') < 300, 'Healthcheck failed'"
          initialDelaySeconds: 60  # Match docker-compose start_period
          periodSeconds: 120       # Match docker-compose interval (5m -> 300s, but use 2m for faster detection)
          timeoutSeconds: 30       # Match docker-compose timeout
          failureThreshold: 3      # Match docker-compose retries
        readinessProbe:
          exec:
            command:
            - python  
            - -c
            - "import os; assert os.path.exists('/app/healthcheck'), 'Healthcheck file missing'"
          initialDelaySeconds: 10
          periodSeconds: 30
      volumes:
      - name: config-volume
        configMap:
          name: rewards-eligibility-oracle-config
      - name: data-volume
        persistentVolumeClaim:
          claimName: rewards-eligibility-oracle-data
      - name: logs-volume
        persistentVolumeClaim:
          claimName: rewards-eligibility-oracle-logs
      restartPolicy: Always